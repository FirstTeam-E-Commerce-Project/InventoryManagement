spring:
  application:
    name: new-inventory-management1

  datasource:
    url: jdbc:postgresql://localhost:5432/new-inventory-management1
    username: postgres
    password: prat9876

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

#eureka:
#  client:
#    register-with-eureka: true
#  service-url:
#    defaultZone: http://localhost:8761/eureka/

server:
  port: 8085

